pipeline {
    agent any
    stages {
        stage('run') {
            steps {
                echo 'CW_Way to Reinvent Yourself'
                sh 'python --version'
                sh 'python pipeline.py'
            }
        }
    }
}